-- Selects and displays the 'score' and 'name' columns from the table 'second_table' where the 'score' is greater than or equal to 10, ordered in descending order by the 'score' column
SELECT score, name FROM second_table WHERE score >= 10 ORDER BY score DESC;
